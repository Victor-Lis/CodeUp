model Challenge {
  id         Int     @id @default(autoincrement())
  
  fileUrl    String

  createdAt DateTime @default(now())

  runs      Run[]
  testCases TestCase[] // Adicione esta linha para a relação

  @@map("challenges")
}