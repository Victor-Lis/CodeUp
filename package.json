{
  "name": "codeup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker-start": "pnpm docker:desktop & pnpm docker:compose",
    "docker-desktop": "docker desktop start",
    "docker-compose:dev-recreate": "docker compose --profile prod down && docker compose --profile dev up -d --build --force-recreate",
    "docker-compose:dev": "docker compose --profile prod down && docker compose --profile dev up -d",
    "docker-compose:prod-recreate": "docker compose --profile prod up -d --build --force-recreate",
    "docker-compose:prod": "docker compose --profile prod up -d --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "@fastify/swagger": "9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "fastify": "^5.4.0",
    "fastify-type-provider-zod": "^5.0.2",
    "zod": "^4.0.10"
  }
}
